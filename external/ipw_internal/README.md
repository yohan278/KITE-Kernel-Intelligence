# IPW Internal Placeholder

Mount or symlink your local IPW repository here if needed:

```bash
ln -s /Users/gabrielbo/projects/hazy/ipw_internal external/ipw_internal
```

KITE can run with local telemetry JSON exports if this path is unavailable.
